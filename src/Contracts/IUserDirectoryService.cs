using SingleAgent.Models;
using SingleAgent.Tools.SingleAgent.Tools;

namespace SingleAgent.Contracts
{
    // Interface for user directory service
    public class IUserDetail
    {
        //Task<UserDetailModel?> GetUserDetailsAsync(string? email, string? employeeId);
        //Task<ApprovalChainModel> GetApprovalChainAsync(string employeeId);
    }
}
